{
  "author": "TMRX",
  "appId": "6a686250ecb3aa66f7e12b14",
  "description": "Configure a product from the Price List catalog, generate a customer quote, push parts to a Work Order, or start a Rental.",
  "version": "3.26.5",
  "versionDescription": "Correction to v3.26.4: 'POS Discount (%)' in bold orange is Calibration Options only — every other section (Base Product, Standard/Custom Options, Startup) reverted back to plain 'Discount (%)', exactly as before. Kept from v3.26.4: on the downloaded PDF, a section's discount rows are omitted entirely when its discount is 0% (applies to all sections, not just Calibration) — on-screen the editable field still always stays visible.",
  "config": {
    "fields": {
      "workflowId": {
        "type": "string"
      }
    }
  }
}